spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/travel
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123456789}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


jwt:
  # the defuat is here but ishould put them in env in production
  secret-key: ${JWT_SECRET_KEY:CFlrMMoGpaR8qKtYTLvrHoRRhO583ZZwhrbr4gB7Bds=}
  expiration-time: ${JWT_EXPIRATION_TIME:36000000}